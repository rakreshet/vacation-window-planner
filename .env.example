# Local development only. Replace this before exposing the app beyond your machine.
POSTGRES_PASSWORD=local-dev-only
# Optional host ports, if the defaults conflict with another local app:
# API_PORT=18080
# WEB_PORT=15173
